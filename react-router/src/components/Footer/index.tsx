import { StyledFooter } from "./styles";
import Logo from "../../assets/logo";

export const Footer = () => {
  return (
    <StyledFooter>
      <Logo />
    </StyledFooter>
  );
};

export default Footer;
